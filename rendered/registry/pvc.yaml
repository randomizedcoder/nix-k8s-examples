apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: zot-storage
  namespace: registry
spec:
  accessModes:
  - ReadWriteOnce
  storageClassName: "local-path"
  resources:
    requests:
      storage: 5Gi

