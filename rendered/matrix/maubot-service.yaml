apiVersion: v1
kind: Service
metadata:
  name: maubot
  namespace: matrix
  annotations:
    argocd.argoproj.io/sync-wave: "6"
spec:
  type: ClusterIP
  selector: { app: maubot }
  ports:
  - { name: http, port: 29316, targetPort: 29316 }

