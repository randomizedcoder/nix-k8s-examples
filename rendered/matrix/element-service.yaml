apiVersion: v1
kind: Service
metadata:
  name: element
  namespace: matrix
  annotations:
    argocd.argoproj.io/sync-wave: "5"
spec:
  type: ClusterIP
  selector: { app: element }
  ports:
  - { name: http, port: 80, targetPort: 80 }

